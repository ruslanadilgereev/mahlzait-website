---
import TermsAndConditionsPage from "@modules/termsAndConditions";
import Layout from "Layout.astro";
import StructuredData from "@components/seo/StructuredData.astro"
import defaultTemplateConfig from "utils/config";
---

<Layout
  {...defaultTemplateConfig}
  seo={defaultTemplateConfig.termsAndConditions?.seo}
>
  <StructuredData config={defaultTemplateConfig} type="page" pageTitle="Terms and Conditions" />
  <TermsAndConditionsPage client:load config={defaultTemplateConfig} />
</Layout>
