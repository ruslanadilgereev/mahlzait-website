---
import CookiesPolicyPage from "@modules/cookiesPolicy";
import Layout from "Layout.astro";
import StructuredData from "@components/seo/StructuredData.astro"
import defaultTemplateConfig from "utils/config";
---

<Layout
  {...defaultTemplateConfig}
  seo={defaultTemplateConfig.cookiesPolicy?.seo}
>
  <StructuredData config={defaultTemplateConfig} type="page" pageTitle="Cookies Policy" />
  <CookiesPolicyPage client:load config={defaultTemplateConfig} />
</Layout>
